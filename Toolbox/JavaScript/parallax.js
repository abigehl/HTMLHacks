var oldx=0;
var oldy=0;
function move(event) {
    if (oldx < event.pageX) {
        //>>
    }
    else {
        //<<
    }

    if (oldy < event.pageY) {
        //down
    }
    else {
        //up
    }
    oldx = event.pageX;
    oldy = event.pageY;
    return null;
};
